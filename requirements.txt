# Runtime dependencies for the pipeline (see requirements-dev.txt for tooling).
pandas>=3.0.5
numpy>=2.5.2
mftool>=3.3
requests>=2.34.2

# Hosted Postgres mirror (Supabase). Optional at runtime -- src/remote_store.py
# imports it lazily and reports a clear error if it is missing -- but it is
# installed by default so the catalogue job and the dashboard share one lockfile.
psycopg[binary]>=3.2

# Dashboard (optional -- the pipeline and reports work without these)
streamlit>=1.61.1
plotly>=6.9.0

# The notebook's dependencies live in requirements-notebook.txt so that hosted
# deployments (Streamlit Community Cloud) do not install jupyter and matplotlib
# on every cold start.
